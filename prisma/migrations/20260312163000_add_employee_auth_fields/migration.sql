DO $$ BEGIN
  CREATE TYPE "EmployeeRole" AS ENUM ('ADMIN', 'PROJECT_MANAGER', 'HR', 'FINANCE', 'DESIGNER');
EXCEPTION
  WHEN duplicate_object THEN null;
END $$;

ALTER TABLE "Employee"
ADD COLUMN IF NOT EXISTS "fullName" TEXT,
ADD COLUMN IF NOT EXISTS "password" TEXT NOT NULL DEFAULT 'admin123',
ADD COLUMN IF NOT EXISTS "role" "EmployeeRole" NOT NULL DEFAULT 'DESIGNER';
